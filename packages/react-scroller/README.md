# @ekoneko/react-scroller

@ekoneko/scroller react provider

## how to use

```tsx
<Scroller options={options}>
  {(scrollerHandler) => (
    <div id="content">
      {/* ... */}
    </div>
  )}
</Scroller>
```
